package repaso;

public class Punto {
    

/*
a) public void Punto() simplemente inicializa los valores de las variables en las coordenadas (0,0), y al crear un nuevo constructor no tiene efecto
ya que no indica a qué punto queremos darle los valores que recogemos al instanciar.
b)El codigo tiene los errores de escritura de que falta el this. en el constructor para indicar a que punto destinamos los datos recogidos, y
el metodo void, tiene el nombre del constructor, lo que a la hora de instanciar, se puede confundir

*/
}
